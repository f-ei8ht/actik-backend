{
  "name": "backend",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "start": "bun run src/index.ts",
    "ingest": "bun run src/ingestion/runner.ts",
    "bench": "bun run scripts/bench.ts",
    "bench:all": "bun run scripts/bench.ts --eval",
    "test": "bun test"
  },
  "dependencies": {
    "hono": "^4.13.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^7.0.2"
  }
}